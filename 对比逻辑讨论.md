# 对比逻辑讨论

## 一、removed 与 deleted 的语义区别

### 1.1 当前代码中的实际用法

从 `src/canmatrix/compare.py` 的 9 处使用来看：

| 行号 | 上下文 | 状态值 | 语义 |
|------|--------|--------|------|
| 76 | 帧在 db1 存在，db2 中名称和 ID 均找不到 | `deleted` | 帧实体消失 |
| 95 | ECU 在 db1 存在，db2 中找不到 | `deleted` | ECU 实体消失 |
| 130 | 值表在 db1 存在，db2 中没有 | `deleted` | 值表实体消失 |
| 159 | 值表中的某个值键在 db2 中不存在 | `removed` | 值条目从值表中移除 |
| 197 | 信号组中某个信号在 db2 中找不到 | `deleted` | 信号从信号组中移除 |
| 211 | 定义列表中的某项定义不存在 | `deleted` | 定义项消失 |
| 252 | 属性键不存在于新对象 | `deleted` | 属性被移除 |
| 300 | 帧内信号在 db2 中找不到 | `deleted` | 信号实体消失 |
| 362 | 帧内信号组在 db2 中找不到 | `removed` | 信号组从帧中移除 |

**结论：当前代码对这两个词的使用并不一致。** 同样是"从容器中消失"：
- 信号从信号组中消失 → `deleted`（行 197）
- 信号组从帧中消失 → `removed`（行 362）
- 值从值表中消失 → `removed`（行 159）

在前端 `compare.js` 中，两者被统一映射为 `'删除'`，说明当前实现**不区分这两个状态的语义差异**。

### 1.2 策略建议：建立清晰的语义标准

将两者的语义重新定义为：

| 状态 | 语义定义 | 判据 | 典型场景 |
|------|----------|------|----------|
| **`deleted`** | 第一类实体（Frame/ECU/Signal/Define）**自身不再存在**于目标数据库中 | 该命名实体在目标数据库中完全找不到 | 帧被删除、ECU 被删除、全局定义被删除 |
| **`removed`** | 第二类实体（成员关系）**从容器中移除**，但父容器仍然存在 | 实体本身可能还存在，但不再属于该容器 | 信号从信号组中移除、值从值表中移除、信号组从帧中移除 |

**判定标准**：
- `deleted` 适用于有独立 `name` 属性的一级实体（Frame、Ecu、Signal、Define）
- `removed` 适用于**集合成员关系变更**（SignalGroup 的成员、ValueTable 的条目、Attribute 的键）

**系统行为差异**：
- `deleted` 实体在对比结果中应作为独立节点出现在树中，标注其完整身份信息
- `removed` 变更应作为父容器的子变更项，展示为"从 X 中移除 Y"

**具体修改建议**：
- 行 197：信号从信号组中移除 → 应改为 `removed`（当前是 `deleted`）
- 行 252：属性键被移除 → 改为 `removed`（当前是 `deleted`，属性是容器的成员）
- 行 211：定义项消失 → 保持 `deleted`（Define 本身是一级命名实体）
- 行 300：信号从帧中消失 → 保持 `deleted`（Signal 是一级命名实体，它"消失"意味着整个信号被删除，不仅仅是移除关系）

---

## 二、"名称不匹配但 ID 匹配"场景的分级判定策略

### 2.1 当前行为

当前代码在 `compare.py` 的处理是：

```
名称匹配? → 是 → compare_frame() 深度对比
          → 否 → ID匹配? → 是 → compare_frame() 深度对比（直接当作重命名）
                         → 否 → "deleted"
```

**问题**：ID 匹配但名称不匹配时，直接走 `compare_frame()` 存在两类风险：
- **假阳性**：相同 CAN ID 被新版本分配给完全不同的报文（如旧版本 ID 0x100 是"车速"，新版本 ID 0x100 是"引擎转速"）
- **假阴性**：报文被重命名但 ID 也变了（如报文从"车速"变为"VehicleSpeed"，同时 ID 从 0x100 变为 0x200）

### 2.2 建议的分级判定管线

```
名称不匹配 + ID 匹配
    │
    ▼
第一级：compare_frame() 深度对比（现有逻辑）
    │  输出：结构差异向量（DLC、收发器、信号数量等）
    │
    ▼
第二级：信号意义相似度分析（新增）
    │  输入：f1.signals 与 f2id.signals 的信号列表
    │  对每个信号进行名称相似度计算
    │
    ▼
第三级：综合判决
    │  根据信号相似度得分 + 结构差异向量 → 分类
```

### 2.3 第二级：信号意义相似度分析

**核心思路**：不依赖单一匹配键，而是计算两个帧的信号列表之间的"语义重叠度"。

#### 2.3.1 信号名称相似度计算

CAN 信号命名有一定规律，通常包含：物理量 + 状态修饰 + 单位信息。建议采用**多策略融合**：

| 策略 | 方法 | 权重 | 说明 |
|------|------|------|------|
| 编辑距离 | Levenshtein 归一化 | 0.25 | `1 - lev(a,b) / max(len(a),len(b))` |
| 公共子串 | 最长公共子串占比 | 0.20 | LCS 长度 / 平均长度 |
| 分词 Jaccard | 按下划线/驼峰分词后求交集 | 0.30 | 对 CAN 信号命名最有效 |
| 缩写展开匹配 | 预定义缩写词典映射 | 0.15 | 如 `VehSpd` ↔ `VehicleSpeed` |
| 数字/单位剥离 | 剥离数字后缀和单位后比较 | 0.10 | 如 `Temp_1` vs `Temp_2` 应视为相关 |

**单信号相似度得分** = 上述策略的加权和，范围 [0, 1]。

#### 2.3.2 帧级信号列表相似度

对两个帧的所有信号，构建**信号匹配矩阵**：

```
帧A信号: [VehSpd, EngRPM, CoolantTemp, BrkPres]
帧B信号: [VehicleSpeed, EngineSpeed, CoolantTemp, BrakePressure]
```

对每对信号计算相似度，然后用**匈牙利算法**（Hungarian Algorithm）找最优全局匹配。得出：

- **匹配信号对数** N_matched
- **平均匹配相似度** avg_sim（只计算匹配上的对）
- **信号覆盖率** = N_matched / max(len(signals_A), len(signals_B))

**帧级信号相似度** = avg_sim × 信号覆盖率

### 2.4 第三级：综合判决标准

| 信号相似度 | 结构差异 | 判决 | 状态 |
|------------|----------|------|------|
| ≥ 0.75 | 任意 | 同一报文，仅名称变更 | `changed`（Name 字段变更） |
| 0.50 ~ 0.75 | DLC 相同 | 同一报文，名称变更 | `changed` |
| 0.50 ~ 0.75 | DLC 不同 | 可疑，标记为"可能相关" | `changed` + warning flag |
| 0.25 ~ 0.50 | 任意 | 追加检查发送器是否一致 | 一致则 `changed`，否则 ID 复用 |
| < 0.25 | 任意 | 完全不同的报文，ID 复用 | 旧帧 `deleted`，新帧 `added` |

**ID 复用判定**（信号相似度 < 0.25）：
- 旧帧标记为 `deleted`（ID 0x100 被释放）
- 新帧标记为 `added`（ID 0x100 被重新分配）
- 在 diff_map 中额外记录 ID 复用关系，供前端展示

---

## 三、区分 "changed"（重命名）与 "deleted"（真消失）的核心策略

### 3.1 问题本质

当前系统面临的困境是**身份识别问题**（Entity Identity Problem）：

```
场景A：报文 "EngineSpeed" 改名为 "EngRPM"，其他不变
  → 应该判定为 changed（名称变更），不是 deleted

场景B：报文 "EngineSpeed" 被删除，新增报文 "EngRPM"，两者完全不同
  → 应该判定为 deleted + added

场景C：报文 "EngineSpeed" 被删除，新增报文 "VehicleSpeed"，ID 恰好复用
  → 应该判定为 deleted + added（ID 复用）
```

当前系统能处理场景 B（名称、ID 均不匹配），但无法区分场景 A 和场景 C。

### 3.2 建议：建立多维度身份指纹

为每个报文/信号建立**身份指纹（Identity Fingerprint）**，包含以下维度：

| 维度 | 字段 | 权重 | 说明 |
|------|------|------|------|
| 名称 | 信号名称分词集合 | 0.30 | 按下划线/驼峰分词 |
| 信号构成 | 信号列表签名 | 0.25 | 信号名称 + 数量 + 布局 |
| 结构属性 | DLC、收发器、周期 | 0.15 | 帧的物理特征 |
| 信号属性 | 各信号的 start_bit、size、factor、offset | 0.20 | 信号布局向量 |
| 数值语义 | 各信号的 min/max、unit、value_table | 0.10 | 物理意义 |

**指纹向量** = 上述维度的归一化特征拼接。

### 3.3 判定管线

```
两个数据库中所有未匹配的帧
    │
    ▼
对每对 (db1中未匹配帧, db2中未匹配帧) 计算指纹相似度
    │
    ▼
构建相似度矩阵 → 匈牙利算法全局最优匹配
    │
    ├── 相似度 ≥ 阈值(如0.7) → 判定为同一实体，名称变更 → changed
    │
    └── 相似度 < 阈值 → 确认删除/新增 → deleted + added
```

### 3.4 阈值选取策略

建议采用**双阈值 + 人工审查区**：

| 区间 | 相似度范围 | 自动判定 | 行为 |
|------|-----------|----------|------|
| 高置信区 | ≥ 0.75 | 自动判定为 renamed | 标记为 `changed`，自动关联 |
| 灰色区 | 0.40 ~ 0.75 | 标记为可疑 | 标记为 `changed` + `suspicious` flag，前端高亮提示用户确认 |
| 低置信区 | < 0.40 | 自动判定为不同实体 | 分别标记 `deleted` 和 `added` |

### 3.5 需要处理的边界情况

1. **一对多重命名**：一个旧帧的信号被拆分到多个新帧
   - 策略：计算旧帧与每个新帧的相似度，取最高者；若多个新帧的相似度都高（如拆帧），标记为 `changed` + `split` flag

2. **多对一合并**：多个旧帧的信号合并到一个新帧
   - 策略：类似地，标记为 `changed` + `merged` flag

3. **信号重排**：帧内信号 bit 位置被重新排列，但信号名称和意义不变
   - 策略：当前 `compare_signal` 已能检测每个信号的 start_bit 变更，但帧级应额外标记"信号布局变更"

4. **缩写与全称**：`VehSpd` ↔ `VehicleSpeed` 应被识别为同一信号
   - 策略：维护一个 CAN 领域缩写词典，在计算名称相似度前做归一化展开

### 3.6 实现建议

建议将新增逻辑封装为独立模块，不改动现有的 `compare_frame` / `compare_signal` 核心逻辑：

```
src/canmatrix/
├── compare.py          # 现有逻辑，保持不变
├── identity.py         # 新增：身份指纹计算
│   ├── build_frame_fingerprint(frame) → vector
│   ├── build_signal_fingerprint(signal) → vector
│   └── fingerprint_similarity(fp1, fp2) → float
├── matcher.py          # 新增：模糊匹配引擎
│   ├── match_entities(list1, list2, threshold) → [(a, b, score)]
│   └── resolve_ambiguities(matches) → resolved_matches
└── cli/compare.py      # 入口处调用 matcher，补充到 compare_db 流程中
```

---

## 四、总结：三个问题的关联

这三个问题本质上是一个问题的三个层次：

1. **术语规范**（removed vs deleted）→ 保证结果语义的正确性
2. **ID 匹配场景判定**（问题二）→ 解决名称变更但 ID 相同的识别
3. **全局去重/关联**（问题三）→ 解决名称和 ID 都不同的跨实体关联

建议按优先级依次推进：先统一术语，再做 ID 匹配增强，最后实现全局身份指纹匹配。